import { forwardRef } from "react";
import type { AccountInfoRef } from "../CreateAccount/CreateAccount";

const CreatePassword = forwardRef<AccountInfoRef>((_props, ref) => {
  return <>CreatePassword.tsx</>;
});

export default CreatePassword;
