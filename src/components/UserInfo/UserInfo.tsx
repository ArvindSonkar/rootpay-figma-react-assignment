import { forwardRef } from "react";
import type { AccountInfoRef } from "../CreateAccount/CreateAccount";


const UserInfo = forwardRef<AccountInfoRef>((_props, ref) => {

    return<>UserInfo</>
})

export default UserInfo;